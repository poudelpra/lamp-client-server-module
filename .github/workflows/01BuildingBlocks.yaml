name: 01-building-block
on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: say Hello
              run: echo "Hello World"
    echo-goodbye:
            runs-on: ubuntu-latest
            steps:
               - name: Failed Steps
                 run: echo "I will fail"
               - name: exit
                 run: exit 1
               - name: Say Goodbuye
                 run: echo "GoodBye"
